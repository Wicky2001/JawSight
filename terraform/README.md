# Terraform Project

This directory contains the Infrastructure as Code (IaC) configuration using Terraform. 
Modules are constructed globally, while specific environments like `dev` and `prod` consume them dynamically via inputs/outputs.
